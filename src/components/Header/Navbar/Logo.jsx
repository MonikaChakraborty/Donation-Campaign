import logo from '../../../Resources/Logo.png'

const Logo = () => {
    return (
        <div>
            <img className='mb-5 md:mb-0' src={logo} alt="" />
        </div>
    );
};

export default Logo;